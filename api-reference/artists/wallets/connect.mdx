---
title: Connect a Social Wallet
openapi: POST /artists/wallets
---

<Note>
  This endpoint requires authentication. You can use either a bearer token in the
  `Authorization` header or an API key in the `x-api-key` header.
</Note>