---
title: Upload to Arweave
description: Upload media content to Arweave permanent storage using the Turbo SDK.
---

<Note>
  Uploads are handled directly by the **Turbo SDK** (`@ardrive/turbo-sdk/web`) from the client. No proxy endpoint is required. This guide targets **Next.js 16**.
</Note>

## Next.js Configuration

The Turbo SDK imports Node.js built-ins (`buffer`, `crypto`, `stream`) that are unavailable in the browser. Configure polyfills in `next.config.js` to prevent runtime errors:

```js
const nextConfig = {
  serverExternalPackages: ["@ardrive/turbo-sdk"],

  turbopack: {
    resolveAlias: {
      buffer: "buffer/",
      crypto: "crypto-browserify",
      stream: "stream-browserify",
      process: "process/browser",
    },
  },

  webpack: (config, { isServer }) => {
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        buffer: require.resolve("buffer/"),
        crypto: require.resolve("crypto-browserify"),
        stream: require.resolve("stream-browserify"),
        process: require.resolve("process/browser"),
      };
    }
    return config;
  },
};
```

## How It Works

The Turbo SDK authenticates with your Arweave wallet key and uploads files directly to Arweave via the Turbo upload service. The SDK handles chunking, retries, and progress tracking internally.

### JavaScript Example

```typescript
import { TurboFactory } from "@ardrive/turbo-sdk/web";

const ARWEAVE_KEY = JSON.parse(
  Buffer.from(process.env.NEXT_PUBLIC_ARWEAVE_KEY, "base64").toString()
);

const turboClient = TurboFactory.authenticated({ privateKey: ARWEAVE_KEY });

const uploadToArweave = async (
  file: File,
  getProgress: (progress: number) => void = () => {}
): Promise<string> => {
  const uint8Array = new Uint8Array(await file.arrayBuffer());

  const { id } = await turboClient.uploadFile({
    fileStreamFactory: () => Buffer.from(uint8Array),
    fileSizeFactory: () => file.size,
    dataItemOpts: {
      tags: [
        { name: "Content-Type", value: file.type },
        { name: "File-Name", value: file.name },
      ],
    },
    events: {
      onProgress: ({ totalBytes, processedBytes }) => {
        getProgress(Math.round((processedBytes / totalBytes) * 100));
      },
    },
  });

  return `ar://${id}`;
};
```

### Browser Fetch Compatibility

The Turbo web SDK passes a `ReadableStream` as the fetch body, which browsers mark as "disturbed" after the first read. Patch the global `fetch` before uploading so the SDK can retry requests:

```typescript
const patchFetch = (): (() => void) => {
  const original = globalThis.fetch;
  globalThis.fetch = async (input, init) => {
    if (init?.body instanceof ReadableStream) {
      const blob = await new Response(init.body).blob();
      return original(input, { ...init, body: blob });
    }
    return original(input, init);
  };
  return () => { globalThis.fetch = original; };
};

// Wrap your upload call
const restoreFetch = patchFetch();
try {
  const uri = await uploadToArweave(file);
} finally {
  restoreFetch();
}
```
